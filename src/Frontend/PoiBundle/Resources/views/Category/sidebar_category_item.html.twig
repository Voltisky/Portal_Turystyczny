<a href="{{ path("frontend_category", {"slug": category.slug, id: category.id}) }}" class="collection-item {% if currentId is defined and currentId > 0 and currentId == category.id %}active{% endif %}" style="padding-left: {{ 30+(depth * 15) }}px;">{{ category.name }}</a>